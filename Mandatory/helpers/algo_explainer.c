// from a to b 
// A target node is the (CLOSEST SMALLER) node in stack B to the current node in stack A)
// i fno "closedst smaller number is found, then the target node is the max value"

// find the cheapest node to push ;
    // SUM  => x operation to bring  'a' to top + x operation to bring 'a'-> target on top = push cost

// from b to a
// A target node is the (CLOSEST LARGER) node in stack A to the current node in stack B)
// if no "closedst larger number is found, then the target node is the min value"
